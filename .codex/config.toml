# Repository-local Codex defaults. Codex applies this file only for a trusted project.
# Keep network and writes outside the workspace behind explicit approval.
sandbox_mode = "workspace-write"
approval_policy = "on-request"

[sandbox_workspace_write]
network_access = false

